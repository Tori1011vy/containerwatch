# Secrets and local runtime configuration
.env
*.pem

# Terraform state and local variable files (the .example file is tracked)
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
.terraform/
crash.log

__pycache__/
